---
tags: [SKN27기, 최종프로젝트, 챗봇, 흐름도, 구현검증본, 발표용]
문서버전: v3.3 (감성분석 확신도 게이트·초단문 바이패스 반영 — 컨텍스트 조회를 분석보다 먼저로 순서 변경)
작성일: 2026-07-02 (v3.3 갱신: 2026-07-03)
설명: "나를 기억하는 친구" 컨셉의 실제 동작 흐름도. 기억 기반 첫인사(재방문), 중요도 기반 즉시 캡처 + 주기 정리 기억 시스템, 대화 연장 장치 5종, 엔딩 클리프행어, 감정 라벨 화면 숨김, MBTI 10초 무조건. 캐릭터 페르소나 프롬프트 제거(이미지·목소리로만 구분). Plan Agent는 스코프 제외.
---

# 웰니스 챗봇 최종 흐름도 — "나를 기억하는 친구" (구현 검증본)

```mermaid
flowchart TD
    START([사용자 진입]) --> GREET["친구 첫인사 생성\n① 기억(재방문: 지난 얘기 이어감)\n② 날씨 ③ 시간대 + 닉네임\n(감정 안 물음 · 질문으로 끝맺음)"]
    GREET --> MAIN["자유 대화 입력"]

    %% MBTI pending 체크 (입력 직후 최우선)
    MAIN --> MBTIPEND{"MBTI 질문 pending?"}
    MBTIPEND -- "아니오" --> CTX
    MBTIPEND -- "예" --> MBTICHK{"방금 질문에 대한 답변인가?"}
    MBTICHK -- "no: 다른 얘기" --> CLR["pending 해제"] --> CTX
    MBTICHK -- "yes" --> MSAVE[("PostgreSQL\nmbti_answers 저장\n(일반 모드 전용)")]
    MSAVE --> MRESP["확인 응답 생성\n(mbti_save 노드에서 직접 · 그래프 종료)"]
    MRESP --> TEXT
    MRESP --> TTS

    %% 컨텍스트 → 감성분석 (뒤에서만 · 화면 라벨 없음) → 톤 결정
    CTX[("컨텍스트 1회 조회\n최근 N턴 + user_memory 요약")]
    CTX --> EMOTION{{"감성분석 (백그라운드)\n초단문→직전 감정 유지\nKcELECTRA+XGBoost 고확신→채택\n저확신→문맥 LLM 재분류"}}
    EMOTION --> ROUTE{"감정 → 톤 선택"}

    ROUTE -- "joy" --> AJOY{{"기쁨 톤"}}
    ROUTE -- "sadness" --> ASAD{{"슬픔 톤"}}
    ROUTE -- "anger" --> AANG{{"분노 톤"}}
    ROUTE -- "normal" --> ANOR{{"일상 톤"}}

    AJOY & ASAD & AANG & ANOR --> RESP["친구 반말톤 응답 생성\n(되묻기로 대화 이어감 · 감정 라벨 화면 표시 안 함)"]

    %% 출력: 텍스트 즉시 + TTS 병렬 (1회 재생 후 파기)
    RESP --> TEXT["텍스트 즉시 출력\n(감정 태그 없음)"]
    RESP --> TTS["ElevenLabs eleven_v3 TTS"]
    TTS --> PLAY["오디오 1회 자동 재생 후 즉시 파기"]
    TEXT --> OUT([사용자에게 전달])
    PLAY --> OUT

    %% 저장 (비동기 · 선순환의 핵심)
    RESP -. "비동기" .-> SESS{세션 모드}
    SESS -- "시크릿" --> RAM[("RAM 캐시\n종료 시 즉시 파기")]
    SESS -- "일반" --> DB[("PostgreSQL\nchat_messages + emotion_label\n+ 기억 즉시 캡처(중요도 기반)\n+ 8턴/종료 시 기억 정리")]

    OUT -.반복.-> MAIN

    %% MBTI 서브플로우 (10초 무입력이면 무조건)
    OUT -. "10초 무입력" .-> TIMER["유휴 타이머 (10초)"]
    TIMER -- "MBTI 수집 미완료\n(일반 모드만 — 시크릿은 질문 안 함)" --> MBTIQ["MBTI 질문 push\n+ pending 설정"]
    MBTIQ -. "다음 입력 대기" .-> MAIN

    classDef agent fill:#E3F2FD,stroke:#1E88E5,stroke-width:2px,color:#0D47A1;
    classDef router fill:#FFF3E0,stroke:#FB8C00,stroke-width:2px,color:#E65100;
    classDef storage fill:#F3E5F5,stroke:#8E24AA,stroke-width:2px,color:#4A148C;
    classDef io fill:#F5F5F5,stroke:#757575,stroke-width:1px,color:#212121;

    class EMOTION,AJOY,ASAD,AANG,ANOR agent;
    class MBTIPEND,MBTICHK,ROUTE,SESS router;
    class CTX,RAM,DB,MSAVE storage;
    class START,GREET,MAIN,CLR,RESP,TEXT,TTS,PLAY,OUT,TIMER,MBTIQ,MRESP io;
```

---

## 단계별 설명 (검증 결과)

### 1. 친구 첫인사 (콜드스타트 폐지)  ✅ 검증완료
감정을 묻지 않는다. 진입 즉시 **닉네임 + 시간대 + 날씨**로 친구가 먼저 말 건다.
브라우저 실측: *"별빛소년아! 점심 뭐 먹었어? 갑자기 궁금해서 물어봤어 ㅎㅎ"* (점심시간 오프너).
날씨 흐림 시 오전="우산 챙겼어?"(챙김) / 오후="비 안 맞았어?"(걱정). → `chat/opener_service.py`

### 2. MBTI pending 체크 (입력 직후 최우선)  ✅ 검증완료
직전에 MBTI 질문을 던진 상태면, 현재 입력이 그 답인지 먼저 판별. 답이면 즉시 저장 후 확인 응답, 다른 얘기면 pending 해제하고 대화로 합류.
**시크릿 모드는 완전 무저장 원칙 — MBTI 질문 자체를 하지 않는다** (저장 못 할 답은 묻지 않음 · 구 "저장 동의 버튼" 플로우 제거, 2026-07-03).

### 3. 감성분석 (뒤에서만) + 톤 결정  ✅ 검증완료
**KcELECTRA + XGBoost** 4감정 분류 + **확신도 게이트** — 원칙: 애매할 땐 찍지 않는다.
① 초단문(10자 미만) → 직전 감정 유지 ② 모델 고확신(≥0.55) → 채택 ③ 저확신 → **최근 대화 문맥을 포함한 LLM 재분류** ④ 실패 시 직전 감정 → normal.
**화면엔 "슬픔 모드" 같은 라벨을 절대 표시하지 않고** 응답 톤 결정과 저장에만 사용.
실측: "김치찌개 먹었어"(화내던 중) → normal ✅ / "몰라…"(우울하던 중) → 슬픔 유지 ✅ / "합격!!" → joy 전환 ✅.

### 4. 친구 반말톤 응답 + TTS  ✅ 검증완료
상담사 X, 반말 친구 O. 되묻기로 대화를 이어간다.
브라우저 실측 응답: *"헐 그건 진짜 너무했다… 너 잘못한 거 하나도 없는데 계속 꼬이면 진짜 사람 미치지. …뭐부터 그렇게 엉킨 건지 하나만 말해봐."*
텍스트 즉시 출력 + ElevenLabs eleven_v3 음성 1회 재생 후 파기.
(예외: MBTI 질문·동의 확인 같은 **고정 문구는 서버 캐시 유지** — `cacheable=True`, 대화 응답만 1회 파기.)

### 5. 저장 (선순환의 핵심)  ✅ 검증완료
- **일반 모드**: `chat_messages`(emotion_label 포함) 매 턴 저장 → 마음리포트·MBTI 분석의 재료
- **기억(`user_memory`) 2단 구조**:
  ① **즉시 캡처(중요도 기반)** — 매 턴 사용자 메시지에서 기억할 가치(예정된 일·사건·인물·취향)를 판별해 그 자리에서 저장. 상대 날짜는 실제 날짜로 변환("다음주 일요일"→2026-07-12). 스몰토크는 버림. 세션이 어떻게 끝나든 유실 없음.
  ② **주기 정리** — 8턴마다/세션 종료 시 압축·중복 제거·끝난 일 정리 (저장이 아니라 청소 담당). 800자 초과 시 자동 압축.
- 이 기억이 재방문 첫인사("면접 준비는 좀 나아졌어?")와 대화 중 회상의 재료 — **"나를 기억하는 친구"의 구현체**
- **시크릿 모드**: RAM 캐시만, 종료 시 즉시 파기 (기억 캡처·첫인사 모두 제외)

### 6. MBTI 서브플로우 (10초 무조건 · 일반 모드 전용)  ✅ 검증완료
**10초 무입력이면 무조건** MBTI 질문 push (강사님 확정 · 턴 수 조건 제거). 답변 시 `mbti_answers` 저장 → 저장된 질문은 다시 안 물음. 시크릿 모드에서는 질문하지 않음. 브라우저 실측: 첫인사 10초 후 MBTI 질문 등장 확인.

### 7. 이번 스코프 제외
| 항목 | 사유 |
|---|---|
| **Plan Agent (Tavily 장소 추천)** | **기능 폐기 — 코드·모델 제거 완료 (2026-07-05)** |
| RAG / GraphDB / 동적 임계값 / 비속어 필터 | 2차 확장 |

---

## 컨셉 — 진짜 친한 친구 (강사님 피드백)
- 감정 캐묻는 상담사 X → **먼저 말 걸어주는 친구** O (날씨/시간/닉네임 첫인사)
- "슬픔 모드" 같은 라벨 화면에서 제거 (분석은 조용히 뒤에서)
- 반말 친구톤 + 되묻기로 대화 많이 유도
- **선순환**: 재밌어서 계속 옴 → 다 저장 → 마음리포트/MBTI로 돌려받음 → 또 옴

## 사용 기술 스택
- **감정분류**: KcELECTRA + XGBoost (4감정)
- **대화 생성**: OpenAI (LangGraph 멀티에이전트)
- **음성**: ElevenLabs eleven_v3 (감정 연기 태그)
- **저장**: PostgreSQL (일반) / 서버 인메모리 (시크릿)
- **캐릭터**: 포리(레서판다)·까미(고양이)·토토(수�