# ═══ 빈틈사이 백엔드 의존성 (2026-07-13 전수 검증 — import 스캔 기준 재작성) ═══

# ── Web Framework ──
Django>=4.2.0,<5.0.0
djangorestframework>=3.14.0
django-cors-headers>=4.0.0
python-dotenv>=1.0.0

# ── Database ──
psycopg2-binary>=2.9.6          # PostgreSQL (대화 원본·세션·리포트)
pgvector>=0.2.0                 # Postgres 벡터 확장 바인딩
redis>=5.0.0                    # 캐시 (settings CACHES)
django-redis>=5.4.0

# ── GraphDB 장기 기억 (NEO4J_URI 설정 시에만 활성, 미설정 시 no-op) ──
neo4j>=5.0.0

# ── LLM / LangGraph (챗봇 멀티에이전트) ──
openai>=1.3.0
langchain>=0.3.0
langchain-core
langchain-openai>=0.2.0
langchain-groq>=0.2.0
langgraph>=0.3.0
requests>=2.31.0                # 외부 API (날씨, TTS: OpenAI/ElevenLabs/Typecast — 별도 SDK 불필요)

# ── 감정분류 추론 (KcELECTRA + XGBoost — ai/emotion) ──
torch
transformers>=4.40.0
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0
numpy>=1.24.0
pandas>=2.0.0                   # 학습·데이터셋 스크립트 (ai/emotion)
matplotlib>=3.7.0               # 시각화 (평가지표 노트북)
seaborn>=0.12.0                 # 시각화 (평가지표 노트북)
cryptography                # DB 채팅 암호화 (Fernet, 팀 안건 2026-07-15)
