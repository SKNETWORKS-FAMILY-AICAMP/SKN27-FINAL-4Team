# ── Web Framework ──
Django>=4.2.0,<5.0.0
djangorestframework>=3.14.0
django-cors-headers>=4.0.0
python-dotenv>=1.0.0

# ── Database (v6.0: PostgreSQL 단일 — Neo4j 제거, Redis는 선택) ──
psycopg2-binary>=2.9.6
redis>=5.0.0
django-redis>=5.4.0

# ── LLM / LangGraph ──
openai>=1.3.0
langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.3.0
requests>=2.31.0

# ── 감정분류 추론 (KcELECTRA 임베딩 + XGBoost — chat/emotion_model.py) ──
torch
transformers>=4.40.0
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0
numpy>=1.24.0

# ── TTS ──
# ElevenLabs REST API (requests 사용, 별도 SDK 불필요)

# ── 제거됨 (v6.0) ──
# neo4j              : GraphDB 2차 확장 (최종_통합_흐름도 §6)
# edge-tts           : 레거시 stream_tts 삭제와 함께 제거 (2026-07-02)
# ChatTTS/vocos/omegaconf/torchaudio/ipywidgets : 미사용 TTS 스택
